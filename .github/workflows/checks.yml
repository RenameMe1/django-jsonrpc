name: checks
run-name: Checks

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
      - run: make test
  mypy:
    runs-on: ubuntu-latest
    steps:
      - run: make lint

