 version: 2

 build:
   os: "ubuntu-24.04"
   tools:
     python: "3"
   jobs:
     pre_install:
       - pip install uv
       - uv sync --only-group docs

 mkdocs:
   configuration: docs/mkdocs.yml
